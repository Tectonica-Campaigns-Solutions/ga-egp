import React from "react";
import { graphql } from "gatsby";
import Layout from "../components/Layout";

const Post = () => {
  return (
    <Layout>
      <h1>Post</h1>
    </Layout>
  );
};

export default Post;
